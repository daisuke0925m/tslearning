export default function arraySample() {
    const colors: string[] = ['red', 'blue']
    colors.push('yellow')
    console.log(colors)

    const even: Array<number> = [2, 3, 4]
    even.push(1, 2)
    console.log(even)
}
